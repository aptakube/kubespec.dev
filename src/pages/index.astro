---
import HomePage from "@components/HomePage.astro";
import SiteLayout from "@layouts/SiteLayout.astro";
import { getProject } from "@lib/kube";

const project = await getProject("kubernetes");
const tag = project.tags[0];
---

<SiteLayout
  title={`Kubernetes Spec ${tag}: Reference Guide and Documentation`}
  description="Find the documentation for all builtin resources, properties, types and even some examples!"
>
  <HomePage project={project} tag={tag} />
</SiteLayout>
